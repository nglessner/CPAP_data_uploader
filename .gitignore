.pio
.vscode/
.kiro/
venv/
__pycache__/
*.pyc
backup_*.tar.gz

# Components (downloaded by setup script)
components/

# Build artifacts
*.o
*.a
*.elf
*.bin
dependencies.lock
sdkconfig*
compile_commands.json

# Auto-generated files
include/version.h

# Temporary files
*.tmp
*.bak
*.swp
*~

# Firmware backups (contain plaintext credentials in NVS)
backups/

# Scratch debugging tools — local PlatformIO sub-projects for isolating
# hardware / BLE / coex issues. Kept out of VCS per session; see #133 for
# the NimBLE + WiFi coex probe methodology if you need to reproduce.
tools/
